source 'https://rubygems.org'

group :jekyll_plugins do
  gem 'jekyll'
  gem 'jekyll-feed'
  gem 'jekyll-sitemap'
  gem 'jekyll-redirect-from'
  gem 'jemoji'
  gem 'webrick', '~> 1.8'
end

gem 'github-pages'
gem 'connection_pool', '2.5.0'

# Gems dropped from Ruby's default set in 3.4/4.0 that Jekyll 3.x still requires.
# Needed only for local builds; GitHub Pages pins its own Ruby.
gem 'csv'
gem 'base64'
gem 'bigdecimal'
gem 'logger'
